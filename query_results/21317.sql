SELECT * FROM ((SELECT DISTINCT title_basics.*, title_ratings.*, name_basics.*, title_crew.*, title_principals.* from title_basics left join name_basics using(nconst), title_rating using(tconst), title_crew using(tconst), title_principal using(tconst)) WHERE ((startyear = 2010 AND genre NOT IN ('Horror')) OR (endyear >= 2010))) LEFT JOIN (SELECT DISTINCT title_basics.*, title_crew.*, title_prinicpal.* from title_basics inner join title_crew ON title_crew.tconst=title_basics.tconst INNER JOIN title_princical USING (tconst))